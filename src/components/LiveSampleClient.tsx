"use client";

import { NegotiatorDashboard } from "./NegotiatorDashboard";

/**
 * The route performs the replay redirect on the server before this mounts.
 */
export function LiveSampleClient() {
  return <NegotiatorDashboard />;
}
